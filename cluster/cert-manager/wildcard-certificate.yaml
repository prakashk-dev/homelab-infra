apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-pkandel
  namespace: kube-system
spec:
  secretName: wildcard-pkandel-tls
  issuerRef: { name: letsencrypt, kind: ClusterIssuer }
  dnsNames: ["*.pkandel.com"]
